cmake_minimum_required(VERSION 2.8)
project( ContourMatching )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( ContourMatching ContourMatching.cpp )
target_link_libraries(ContourMatching ${OpenCV_LIBS})
